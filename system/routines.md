# ⚙️ 루틴 운영 가이드 — 자동 조립라인 (수집→글쓰기→검사) + 사람 발행

> 이 블로그가 "글 한 편"을 만드는 전체 흐름을 **3개 자동 루틴 + 1개 사람 게이트(발행)**로 정리한 **운영 정본**.
> 아침 6시 수집(Haiku) → 7시 글쓰기(Opus 4.6) → 8시 검사(Opus 4.8)가 자동으로 돌고, **사람은 뒤에서 최종검토·발행만** 한다.
> 관련: `system/daily-collection.md`(루틴 ① 플레이북) · `system/routine-write.md`(루틴 ② 정본) · `system/routine-verify.md`(루틴 ③ 정본) · `system/writing-guide.md`(글쓰기 기준) · `prompts/`(단계별 프롬프트) · `build-seo.mjs`(발행 빌드).

---

## 한눈에 — 상태가 흐르는 길

```
   루틴 ① 06:00       루틴 ② 07:00        루틴 ③ 08:00         🚦 사람
   수집(Haiku)        글쓰기(Opus 4.6)     검사(Opus 4.8)       최종검토·발행
뉴스 ─▶ [인박스]────▶ 그날 뉴스 초안 ────▶ 검증·수정 v2 ──────▶ 내가 확인 ─▶ [발행]
      3탭 구축         (검증·자료완비        과단정 스캔+           OK한 것만    빌드·배포
      verified          중 최대 2편)         팩트 재검+리포트
        │                  │                    │                  │
      inbox.json      drafts/v1.md          drafts/v2.md        build-seo.mjs
      collect/*.md    stage:작성중          REVIEW.md·검수중
```

| 상태 | 저장 위치 | 만드는 주체 |
|---|---|---|
| 인박스 + 3탭(정리본·자세히·반응) | `inbox.json` · `collect/*.md` · `collect/reactions/*.md` | **루틴 ①** Haiku |
| **작성중** (초안 v1) | `drafts/<slug>/v1.md` + `posts.json` 등재 | **루틴 ②** Opus 4.6 |
| **검수중** (검증본 v2 + 리포트) | `drafts/<slug>/v2.md` · `REVIEW.md` | **루틴 ③** Opus 4.8 |
| **발행** (정적 페이지·OG·sitemap·feed) | `p/<slug>/…` 등 | **🚦 나 (사람)** + `build-seo.mjs` |

> **큰 그림**: 사람이 손대는 곳은 **딱 하나 — 맨 뒤 발행 승인**. 무엇을 쓸지(선정)는 "그날 수집된 검증·자료완비 뉴스 최대 2편"이라는 규칙으로 자동화했다.

---

## 🔵 루틴 ① — 매일 수집  *(Claude 스케줄 트리거, KST 06:00)*

| 항목 | 내용 |
|---|---|
| **모델** | **Haiku 4.5** (수집·정리·점수 = 기계적). |
| **하는 일** | **0단계 발행 브랜치 강제 이동** → 8각도 웹 검색 → 후보 추출·중복 제거 → 날짜 검증(2곳+ 교차) → 1차 점수·태깅 → **수집한 모든 verified 항목에 3탭 전부**(정리본·자세히·반응) → `inbox.json` append → **검증 게이트(`validate-collection.mjs`) 통과** → 커밋·푸시(곧장 발행 브랜치) |
| **정본 프롬프트** | 트리거에 인라인(점수 `prompts/A` · 정리본·자세히 `prompts/J` · 반응 `prompts/H`) |
| **끝점** | "아침에 열면 **검증된 후보가 쌓여 있는** 상태". 글은 안 쓴다. |
| **가드** | 미확인 시 `verified:false`. 수집=3탭 필수(탭 없이 인박스만 금지). 커밋 전 `validate-collection.mjs` 초록불 필수 — **발행 브랜치 아니면 HARD FAIL(임시 브랜치 커밋 차단)**. `surfaced`=오늘 기록(글쓰기 루틴의 "그날 뉴스" 판정 기준). |

## 🟠 루틴 ② — 자동 글쓰기  *(Claude 스케줄 트리거, KST 07:00)*  → 정본 `system/routine-write.md`

| 항목 | 내용 |
|---|---|
| **모델** | **Opus 4.6** |
| **대상 (자동 선별)** | `inbox.json`에서 **① `surfaced`=오늘 ② `verified:true` ③ 3탭 자료 완비 ④ `postId` 없음** 을 만족하는 항목. **하루 최대 2편**(3개+ 이면 임팩트 큰 2편, 나머지는 다음 날). |
| **하는 일** | 대상마다: 교차검증(`prompts/K`) → 초안(`prompts/I` + `writing-guide.md`) → `drafts/<slug>/v1.md` → `posts.json` 등재(`stage:"작성중"`) → `inbox.json`에 `postId` 연결 |
| **끝점** | 초안 v1이 `작성중`으로 저장된 상태. |
| **절대 규칙** | ❌ build-seo·발행 금지 ❌ 2편 초과 금지 ❌ `검수중`/`발행`으로 안 올림. ✅ **0단계 발행 브랜치 강제 이동** → 커밋·푸시. |

## 🔴 루틴 ③ — 자동 검사(검증)  *(Claude 스케줄 트리거, KST 08:00)*  → 정본 `system/routine-verify.md`

| 항목 | 내용 |
|---|---|
| **모델** | **Opus 4.8** (검사자 ≥ 작성자여야 실수를 잡는다). **새 세션**(자기 글 옹호 편향 차단). |
| **대상** | `posts.json`에서 `stage:"작성중"` 전부(루틴 ②가 넘긴 것). |
| **하는 일** | 대상마다: **과단정 스캔(규칙 17)** + 정직성·리스크(9·15) + 정밀표기(16) + **웹 팩트 재검** → 규칙 기반 수정 적용 `drafts/<slug>/v2.md` → `REVIEW.md`(고친 것·남은 리스크·사람 판단 사안) → `stage:"검수중"` |
| **수정 원칙** | 방향은 거의 항상 **'낮추기'**(수치 정성화·단정은 귀속·감정어는 중립어·최상급 삭제). 단 '한쪽만'은 반대 소스 보강. **문체·목소리 보존**, 앵글·taste는 안 건드리고 플래그만. |
| **끝점** | v2 + 리포트가 `검수중`에 대기. "N편 검수 준비됨" 알림. |
| **절대 규칙** | ❌ build-seo·발행 금지 ❌ 사실 조작 금지 ❌ `발행`으로 안 올림. ✅ **0단계 발행 브랜치 강제 이동** → 커밋·푸시. |

## 🚦 사람 게이트 — 발행 (자동화 금지 구역)

- `검수중`의 **v2 + REVIEW.md**를 읽고 최종검토·피드백(필요시 v3).
- OK하면: `posts.json` `stage:"검수중" → "발행"` + `published`(오늘) + **🔒 `publish-approvals.json`에 승인 등록** → **`node build-seo.mjs`** → 커밋·푸시.
- **여기가 유일한 사람 게이트.** 승인 없이는 어떤 글도 라이브로 안 나간다 — 이제 **산문이 아니라 기계가 강제**한다: `stage:"발행"`이어도 `publish-approvals.json`에 없으면 build가 건너뛰고 대시보드도 발행으로 안 띄운다. **승인 등록은 사람이 "발행해" 지시할 때만**(루틴·자동 금지).

---

## 왜 이렇게 끊었나

루틴을 끊는 기준은 "일이 많아서"가 아니라 **경계가 진짜 있는 곳**:
1. **모델 티어가 바뀜** — ① Haiku(싸게 대량) → ② Opus 4.6(초안) → ③ Opus 4.8(강한 검사) → 발행 코드(무료).
2. **독립성이 필요함** — ②가 쓴 걸 ③이 **다른 세션·더 강한 모델**로 검사해야 자기 글 옹호를 피한다.
3. **사람이 껴야 함** — 맨 뒤 발행 승인.
4. **실패 격리** — ③이 터져도 초안(②)은 안 날아가고, 발행이 터져도 원고는 무사.

## 비용 원칙 (요약)

- **수집=Haiku, 글쓰기=Opus 4.6, 검사=Opus 4.8, 발행=코드(무료).**
- **하루 상한 2편**으로 Opus 사용을 캡. 대상 0편인 날은 루틴이 즉시 종료(비용 0).
- **프롬프트 캐싱**: 매 글마다 붙는 `writing-guide.md` + 골드 스탠다드를 캐시(~90%↓).

---

## 만들 것 (구축 체크리스트)

- [x] **1. `system/routines.md`** — 이 문서 (기획 확정본)
- [x] **2. 루틴 ② 정본** — `system/routine-write.md` 신설
- [x] **3. 루틴 ③ 정본** — `system/routine-verify.md` 신설
- [x] **4. 루틴 ① 스케줄 트리거** — 등록 완료. 매일 KST 06:00(`0 21 * * *` UTC), Haiku 4.5, 새 세션, 완료 알림. 트리거 ID `trig_01RkzYDfDBo3yQ2vdu7yutme`.
- [x] **5. 루틴 ② 스케줄 트리거** — 등록 완료. KST 07:00(`0 22 * * *` UTC), 새 세션, 완료 알림. 트리거 ID `trig_01981abhykq97RLnHov65mWT`. ⚠️ **모델은 UI에서 Opus 4.6으로 지정**(API 모델변경 비활성).
- [x] **6. 루틴 ③ 스케줄 트리거** — 등록 완료. KST 08:00(`0 23 * * *` UTC), 새 세션, 완료 알림. 트리거 ID `trig_01CnrKhjcxhM5DdmzvXmh4Yz`. ⚠️ **모델은 UI에서 Opus 4.8으로 지정**(API 모델변경 비활성).
- [x] **7. 대시보드 상태 뱃지** — 인박스→작성중·검수중→발행 파이프라인이 inbox.json/posts.json 실카운트 반영.

## 가동 상태 (2026-07-16 기준)

- ✅ **트리거 3개 모두 등록·활성** (환경 `env_0115kRrryBnGvx7sBgUnXJii`, 새 세션, 완료 알림 push+email):
  - 루틴 ① 수집 `trig_01RkzYDfDBo3yQ2vdu7yutme` — `0 21 * * *` UTC(06:00 KST) — 모델 **Haiku 4.5** ✅
  - 루틴 ② 글쓰기 `trig_01981abhykq97RLnHov65mWT` — `0 22 * * *` UTC(07:00 KST) — 모델 **미지정 ⚠️**
  - 루틴 ③ 검사 `trig_01CnrKhjcxhM5DdmzvXmh4Yz` — `0 23 * * *` UTC(08:00 KST) — 모델 **미지정 ⚠️**
- ✅ 정본 문서·프롬프트 정합화 완료(writing-guide 17규칙+클리셰, checklists 검수 반영, positioning/analysis-frameworks 연결, prompts I·K·F 정리).
- ⚠️ **가동 전 남은 단 하나 — 대시보드 UI에서 모델 지정**: 루틴 ② → **Opus 4.6**, 루틴 ③ → **Opus 4.8**. (API 모델변경 비활성이라 UI에서만 가능. 안 하면 기본 모델로 돌아 원하는 티어가 안 맞음.)
- 🔎 운영 관전 포인트: ②는 ①이, ③은 ②가 **끝나 push한 뒤** 이어받는다(각 1시간 간격). 어느 날 앞 단계가 오래 걸려 뒤 단계가 "대상 0건"이면 그날은 건너뛰고 **다음 날 자동 이어받음**(자가치유). 자주 비면 간격을 넓힌다.

## 변경 로그
- v6 (2026-07-20): **중복 재수집 방지 — 기계 탐지**. 산문 대조(제목·URL 눈으로)가 표현 차이를 놓쳐 같은 소식(Muse·Kimi·GPT-5.6·Google소송 등)이 매일 재수집되던 문제. ①2단계 중복 제거를 3기준(source URL·핵심 고유명사·이미 글 된 주제)으로 강화. ②`validate-collection.mjs`가 제목 토큰 자카드≥0.5로 근접 중복 탐지 — 신규(surfaced≥2026-07-21)가 기존과 제목 유사하면 **HARD FAIL(재수집 차단)**, 기존 누적 중복은 WARN(정리 권장). 같은 URL만 겹치는 건 오탐 방지로 WARN.
- v5 (2026-07-20): **수집 = 3탭 필수(escape 폐지)**. 그동안 '여력 안 되면 탭 없이 인박스 후보로만' 허용이 있어 stub만 쌓였다(8개 수집 중 3개만 탭 만든 사례). 이제 **인박스에 등재하는 verified 항목은 예외 없이 3탭**을 만들고, 못 만들 소식은 아예 등재하지 않는다. 산문이 아니라 검증기로 강제: `validate-collection.mjs`가 `surfaced >= 2026-07-21`인 verified 무탭 항목을 **HARD FAIL**(과거 stub은 소급 유예). daily-collection·routine-prompt 5단계 동시 수정.
- v4 (2026-07-17): **임시 브랜치 커밋 사고 방지 — 두 겹 안전장치**. (사고: 루틴 ①이 수집물을 세션 임시 브랜치 `claude/jolly-feynman`에 커밋·push해 대시보드에 안 뜸. 원인 = 브랜치 동기화가 '맨 끝 산문 지시'라 건너뛸 수 있었음.) ①**0단계 발행 브랜치 강제 이동**을 세 루틴 모두의 첫 동작으로 신설(임시 브랜치에서 시작하든 곧장 발행 브랜치로 옮겨 작업). ②**`validate-collection.mjs`에 브랜치 가드** — 발행 브랜치가 아니면 커밋 전 HARD FAIL. 옛 'cherry-pick 동기화(구 10단계)'는 폐지. → 산문이 아니라 기계가 강제.
- v3 (2026-07-16): **글쓰기·검사를 각각 자동 루틴으로 분리**. 3-루틴(수집·글쓰기·발행, ②③ 수동) → **자동 조립라인**(수집 Haiku 06:00 → 글쓰기 Opus 4.6 07:00 → 검사 Opus 4.8 08:00)으로 개편.
  사람 선정 게이트 폐지 → "그날 수집된 검증·자료완비 뉴스 최대 2편" 자동 선별. 사람 게이트는 맨 뒤 발행 승인 하나만 남김.
  검사자(4.8)를 작성자(4.6)보다 강한 모델·다른 세션으로 둬 자기 글 옹호 차단. `routine-write.md`·`routine-verify.md` 정본 신설.
- v2 (2026-07-16): 루틴 ①에 **"탭 만든 소식은 3탭 전부"** 규칙 + **커밋 전 검증 게이트**(`validate-collection.mjs`) 반영.
- v1 (2026-07-10): 최초 작성. 3-루틴+게이트 구조 확정.
