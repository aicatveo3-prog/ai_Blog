# 운영 워크플로 — 매주·매월 무엇을 하는가

> 이 파일은 "오늘 뭐 하지?"에 답하는 실행 체크리스트다. 상세 근거는 [`docs/manual.md`](docs/manual.md).
> 핵심: **요일별로 "단계"를 고정한다.** 매일 전 단계를 오가면 컨텍스트 전환 비용으로 시간이 1.5배 든다.

---

## 항목 상태 머신
```
인박스 → 검토중 → 선정 → 조사중 → 작성중 → 검수중 → 발행
                ↘ 보류(에버그린 후보)    ↘ 폐기(사유 기록)
```

---

## 주간 캘린더 (주 8~12시간 모델)

| 요일 | 작업 | 시간 | 사용 문서 |
|---|---|---|---|
| **월** | 편집회의(선별·슬롯 배정·관점 한 줄) + 브리핑 1 발행 | 1.5h | `scoring-rubric.md` |
| **화** | 심층 글: 원문 조사 + 커뮤니티 반응 정리 | 2h | `research-note.md` |
| **수** | 심층 글: 직접 테스트 (표준 시나리오) | 2~3h | `test-protocol.md` |
| **목** | 심층 글: 프롬프트 체인(P1~P4) + P5 사람 편집 | 2~3h | `prompts/` |
| **금** | 검수·발행 + 브리핑 1 + 주간 종합(D) 발행 | 1.5h | `checklists.md`, `prompts/G` |
| **주말(격주)** | 에버그린 튜토리얼 작업 | 3~4h | `post-templates.md` C |

---

## 한 편이 만들어지는 흐름 (글 하나 기준)

1. **선별** — 인박스에서 상위 20% 훑고 슬롯 배정, 관점 한 줄 메모 → 상태 `선정`
2. **조사** — `research-note.md` 복사해 A~G 채우기 (직접 테스트 포함) → 상태 `조사중→작성중`
3. **관점 확정** — `prompts/B` 로 각도 10개 발산 → 사람이 1개 골라 논지 확정
4. **초안 체인**:
   - P1 `prompts/C` 팩트 추출
   - P2 `prompts/D` 아웃라인 2안 → **★사람 승인★**
   - P3 `prompts/E` 섹션 작성
   - P4 `prompts/F` 에디터 비판 (**새 대화에서**)
   - P5 사람 편집 (전체 시간의 30~40%)
5. **검수** — `checklists.md` 3종 통과 → 상태 `검수중`
6. **발행 게이트** — 마지막 3질문. 하나라도 "아니오"면 보류 → 통과 시 상태 `발행`

---

## 월간 (월 1회, 1시간) — 역분석
1. 상위 3편·하위 3편을 "유형·소스·관점·제목" 네 변수로 비교.
2. 결론을 세 군데에 반영:
   - `scoring-rubric.md` 가중치
   - `prompts/` 프롬프트·템플릿
   - `scoring-rubric.md` 킬 기준
3. 각 변경마다 해당 파일의 "변경 로그"에 **왜 바꿨는지 한 줄** 남긴다.

> **30일 룰**: 모든 글은 발행 30일 후 지표(체류시간·완독률·재방문·구독전환·검색어질)를 한 번 기록한다. 발행 직후 지표는 유통의 함수라 퀄리티 판단에 못 쓴다.

---

## 3개월 성공 판정 체크리스트
- [ ] 주간 리듬(브리핑 2~3 + 심층 1)이 4주 연속 유지됐다
- [ ] 발행 글의 절반 이상에 직접 테스트·실측 데이터가 있다
- [ ] 뉴스레터 구독자가 0이 아니고 우상향 중이다
- [ ] 역분석 결과가 프롬프트/루브릭에 최소 1회 반영됐다
- [ ] 편당 제작 시간이 2주차 대비 30% 이상 줄었다 (품질 유지 조건 하)
